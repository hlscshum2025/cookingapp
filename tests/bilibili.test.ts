import{describe,expect,it}from"vitest";import{parseBilibiliExport}from"../lib/bilibili";describe("Bilibili import",()=>{it("accepts exporter videos",()=>{const r=parseBilibiliExport({videos:[{bvid:"BV1abc",title:"菜谱",url:"https://www.bilibili.com/video/BV1abc",uploader_name:"UP"}]});expect(r).toHaveLength(1);expect(r[0].externalId).toBe("BV1abc")});it("drops unusable rows",()=>expect(parseBilibiliExport({videos:[{title:"bad"}]})).toEqual([]))});
