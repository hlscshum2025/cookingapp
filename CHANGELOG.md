# CookingApp 版本与项目总日志

最后更新：2026-08-11  
当前基线：`v1.2.0+20260811.03`

本文是 CookingApp 的统一版本日志和项目进度索引。它记录已经完成、已经部署、仍需真实验收和已推迟的事项。正式菜谱、私人日志、备份和密钥不进入 GitHub。

## 最新发布记录

### v1.2.0+20260811.03 - 2026-08-11

- 修正注册确认邮件与密码找回混用的流程，新增带 Turnstile 的注册确认邮件重发入口。
- 手动录入加入可收起的悬浮 B 站播放器，桌面和手机滚动时可以继续观看。
- 页面切换采用空闲预加载；后台同步不再遮住整个页面；菜谱保存后改为站内跳转。
- 运行时公开配置短时缓存，并为日志、导入关联、标签和菜谱版本补齐数据库索引。
- Supabase 认证日志确认此前请求实际进入的是密码恢复接口；内置邮件服务仍有全项目每小时2封限制，正式多人使用前需配置自定义 SMTP。
- 27 项功能与安全测试、完整构建、Supabase 安全及性能检查通过；新版已部署到正式域名。

### v1.1.0+20260811.02 - 2026-08-11

- 将视频审核、JSON 来源导入和手动录入合并到导入中心。
- 来源视频列表改为读取当前用户的 `source_videos`，支持搜索、内嵌播放和打开 B 站原链接。
- 一键手动录入自动带入 BV号、标题、UP主、链接、简介、封面和时长。
- 保留第三版“一键自动导入”入口，第二版以人工核验流程为主。

## 1. 版本号规则

CookingApp 从 `v1.0.0+20260811.01` 开始采用 **SemVer（语义化版本）+ 日期构建号**：

```text
v主版本.次版本.修订版本+YYYYMMDD.当日序号
```

示例：

```text
v1.0.0+20260811.01
v1.1.0+20260818.01
v1.1.1+20260818.02
v2.0.0+20260915.01
```

含义：

- `主版本 MAJOR`：产品阶段或兼容方式发生明显变化，例如正式进入 V2、数据接口不兼容。
- `次版本 MINOR`：新增向后兼容的功能，例如购物清单、统一导入入口。
- `修订版本 PATCH`：修复 Bug、安全问题、文案或小范围界面调整。
- `YYYYMMDD`：实际发布或形成可交付版本的日期。
- `当日序号`：同一天第几次发布，从 `01` 开始。

### 提交、标签和日志格式

- Commit：`[v1.1.0+20260818.01] Add shopping list export`
- Git tag：`v1.1.0+20260818.01`
- CHANGELOG 标题：`## v1.1.0+20260818.01 - 2026-08-18`
- Sites 发布、GitHub 提交和本文件应使用同一个版本号。

每次新版必须记录：

1. 新增功能；
2. 修改和修复；
3. 数据库或环境变量变化；
4. 安全与隐私影响；
5. 验证项目和结果；
6. 已知限制与下一步。

仅修改文档但没有发布网站时，也可增加 PATCH 版本，但必须写明“未重新部署”。

## 2. 当前系统全貌

| 部分 | 当前方案 | 状态 |
|---|---|---|
| 产品 | 手机优先的个人视频菜谱知识库 | 已形成可用网页 |
| 前端与应用 | Next.js / React / TypeScript，ChatGPT Sites 托管 | 已部署 |
| 正式网址 | `https://cookingapp.caoxiaoysm.com` | 已绑定并启用 HTTPS |
| 域名与 DNS | 腾讯云 `caoxiaoysm.com` | 已购买并使用子域名 |
| 登录与数据库 | Supabase Frankfurt，Auth + PostgreSQL + RLS | 已连接 |
| 业务数据 | 10 张业务表，按用户所有权隔离 | 已启用 RLS |
| 视频 | 保存 BV 号、链接和元数据；播放时打开 B 站官方播放器 | 不下载视频 |
| 图片 | 规划使用 Supabase 私有 Storage | 需继续实机验收 |
| 代码与文档 | GitHub `hlscshum2025/cookingapp` | 持续更新 |
| 正式私有数据 | Supabase 与个人备份 | 不进入公开 GitHub |

当前网页入口已公开，公开的是登录页面，不是私人菜谱。未登录用户和其他账号不能读取当前用户的数据。

## 3. 产品原则与已确定边界

- 核心对象是“亲自做过、验证成功、持续修改”的菜谱，不追求完整搬运视频平台。
- B 站视频仍保留在 B 站；CookingApp 只保存链接、BV 号、标题、作者、封面链接、字幕/证据和整理后的菜谱信息。
- 第二版主流程确定为：在软件内打开 JSON 对应的 B 站原视频，由用户手工录入和校对。
- 自动转写、OCR、视频理解、自动结构化和批量处理推迟到第三版。
- 不上传 B 站 Cookie，不自动爬取受限内容，不把视频文件复制到 Supabase。
- 可追溯、可编辑、可备份和人工核验优先于全自动。
- 健康、无麸质、过敏原和替代品信息必须保留来源与核验状态，不作为医疗建议。
- 先稳定响应式 Web，再决定封装微信小程序；微信登录和多人投稿不阻塞第二版。

## 4. 阶段状态（截至 2026-08-11）

### 阶段 0：构思、规格和技术路线 — 已完成

- 明确个人视频菜谱库定位、用户端和管理端需求。
- 确定 GitHub 保存代码与文档，Supabase 保存账号和正式业务数据。
- 确定 Next.js / React / TypeScript + Supabase + Sites 技术路线。
- 完成产品功能、数据库结构、技术栈、开发计划和食材映射表等基础文档。
- 设计“来源视频 → 当前个人版本 → 多次做菜日志”的三层内容结构。
- 预留成本、时间、德国采购、多灶台和多厨房调度接口。

### 阶段 1：工程骨架、数据库和云端验收 — 大部分完成

已完成：

- 响应式网页、总览、菜谱库、详情、编辑、删除、标签、厨房模式和设置页面。
- Supabase migration、10 张业务表、私有 Storage 策略和用户所有权字段。
- 邮箱登录、密码登录、注册、忘记密码、重置密码和修改密码界面。
- 云端真实读写已经连通；当前数据库中能看到 14 条菜谱记录。
- 退出登录后不再保留可直接浏览的私人菜谱；本地缓存按登录用户隔离。
- 完整 JSON 备份覆盖 10 张业务表；图片与数据库备份分开。
- RLS 真实越权矩阵 `115/115` 通过，覆盖匿名读取、第二用户横向读写、跨用户关联、`owner_id` 篡改、私有 Storage 和业务 RPC。
- 数据库内部触发器函数已撤销匿名和普通登录用户的直接执行权限。

仍需实机确认：

- 上传、读取和删除一张私有图片的完整流程。
- 在空测试项目完成一次 migration 重建、JSON 恢复和 Storage 恢复演练。
- 断网、保存失败、令牌过期、重复提交和重试的完整用户体验。
- 使用第二个真实账号再次验收多用户数据隔离。

### 阶段 2：公网部署、域名和账号安全 — 大部分完成

已完成：

- ChatGPT Sites 公网部署，不依赖个人电脑开机。
- 正式域名 `cookingapp.caoxiaoysm.com`、HTTPS 和 Supabase 回调地址。
- Sites 的 ChatGPT 私有门禁已移除，普通浏览器可直接打开应用登录页。
- Supabase URL 与 Publishable key 已作为生产环境配置加载。
- Cloudflare Turnstile Site Key 已接入前端；登录、注册和密码找回会传递 CAPTCHA token。
- 密码至少 12 位；注册和登录分离；管理者提示缩小。
- Supabase 当前关闭“允许新用户注册”时，注册页面可见但不会创建新账号。
- Supabase 免费版不能启用泄露密码保护；当前以强密码、邮箱确认、限流和 CAPTCHA 作为替代控制。
- 会话恢复改为更安静的后台式同步，避免重复读取整套云端数据。

已知限制：

- 中国大陆访问 Sites / OpenAI 托管链路可能需要 VPN；这不是 Supabase 法兰克福数据库单一导致的问题。
- 德国使用 Sites + Frankfurt Supabase 更符合当前主要使用地，因此暂不迁移德国服务器。
- 域名注册不等于服务器托管；中国大陆服务器通常需要 ICP 备案。ICP备案本身免费，但需要符合条件的大陆托管资源并等待审核。
- 暂不为短期国内访问购买大陆服务器；到德国后先完成真实网络验收。

仍需确认：

- Turnstile Secret Key 已只配置在 Supabase 的 CAPTCHA 设置中，未进入代码或 GitHub。
- 德国手机网络下完成登录、编辑、退出、重新打开和同步验收。
- 正式开放注册前检查邮箱确认、限流、自定义 SMTP 和第二账号隔离。

### 阶段 3：首批真实内容闭环 — 进行中，不能仅按记录数判定完成

已有进展：

- 收藏夹 JSON 已核验约 199 条视频，早期抽样显示 0 条失效。
- 前 10 条元数据可导入，4 条需要优先人工核验。
- 当前 Supabase 中已有 14 条菜谱/待整理记录。
- 两条 B 站字幕样本已经验证：`BV1CQ4y1j7or` 183 段，`BV1JmbVzfEev` 76 段。
- 已有视频审核页、B 站内嵌播放器、字幕 JSON 读取和手动录入工作台。

完成标准仍是至少 10 道菜分别具备：

1. 来源视频和 UP 主信息；
2. 食材、用量、厨具、步骤、时间和火候；
3. 未知字段明确标为待核验；
4. 自己的修改版本；
5. 至少一次真实制作日志；
6. 重新打开、搜索、标签和厨房模式验证。

因此，“数据库有 14 条记录”不等于阶段 3 已完成。下一轮应逐条做闭环盘点。

## 5. 当前已实现的功能模块

### 菜谱与个人知识

- 菜谱新建、编辑、删除、详情、状态和可见性。
- 分组食材、用量、厨具、步骤、份数、难度、时间和标签。
- 来源视频、当前个人版本和历次制作日志。
- 厨房模式：大字号步骤、逐步勾选和份数换算。
- 做菜日志：日期、评分、本次调整、结果、下次改进和照片字段。

### 导入与视频审核

- 读取 B 站收藏夹 JSON、按 BV 号去重、预览、导入任务和逐条审计。
- `source_videos / import_jobs / import_items` 数据链路和原子写入函数。
- 页面内使用 B 站官方播放器，不保存或转码视频文件。
- 手动录入来源、字幕、画面证据、食材、步骤和核验状态。
- 第二版不再把自动采集作为发布前提；导入中心以“选择视频 / 手动录入”的统一入口为目标。

### 账号、安全和隐私

- 邮箱 + 密码登录、注册、忘记密码、重置密码、修改密码。
- 邮箱确认、12 位密码要求、Turnstile 验证链路。
- 10 张业务表 RLS、多用户所有权隔离、跨用户关联防护。
- 未登录页面统一门禁；退出后清除当前设备上的私人缓存。
- Secret Key、数据库密码和 `service_role` 永不进入前端或 GitHub。

### 手机端导航

手机底部五个主要入口确定为：

1. 标签总览；
2. 导入中心；
3. 食材词典；
4. 翻译采购；
5. 成本核算。

其余功能放入“更多”菜单：总览、菜谱库、视频审核、手动录入、做菜日志和设置。桌面端继续显示完整导航。

### 其他已具备或已有骨架

- 中英德食材词典、德国采购关键词、无麸质和人工确认状态。
- 翻译采购分区和成本核算代码骨架。
- 标签搜索和分类界面基础。
- 公开只读分享页、二维码与复制链接骨架；匿名数据库读取目前关闭。
- PWA manifest 和完整 JSON 数据导出。

## 6. 后续版本范围

### V2：初步日常应用版

目标：能直接打开正式链接日常使用，也保留后续封装微信小程序的可能。

优先事项：

- 把阶段 3 的至少 10 道真实菜谱闭环做完。
- 统一“导入中心”和“手动录入”：先选 B 站 JSON 中的视频或粘贴链接，再观看原视频并手工填写。
- 标签总览改为搜索引擎式入口，覆盖中餐、西餐、早餐、正餐、零食、面食、硬菜等维度，并支持自定义标签。
- 准备可扩展的中英德基础食材词典；未收录项再调用在线翻译，并保留人工确认状态。
- 翻译采购记录中文食材、德国名称、品牌、超市、来源地区和更新时间。
- 增加类似购物车的选菜功能，一键汇总去重后的采购清单。
- 购物清单先支持复制文本和系统分享，可手动发送到备忘录/记事本；网页不在未经确认时直接写入系统笔记。
- 完成备份恢复、手机主流程、错误重试和第二账号隔离验收。
- 根据德国实际使用体验修正德文商品名称和采购关键词。

### V3：自动化与批量版

- 批量整理约 199 条收藏内容，每批 20–30 条。
- 自动音频转写、B 站字幕处理、关键帧 OCR 和视频理解。
- AI 只生成候选字段；克数、温度、时间和健康信息仍需人工确认。
- 导入失败重试、断点续传、重复检测和批量审核。
- 来源版与个人版差异比较、细粒度版本回滚。
- 进一步评估微信小程序、微信登录和多人投稿审核。

### 更长期候选

- 周菜单、库存、价格历史、营养估算和个人忌口规则。
- 自动超市搜索、商品库存和替代品建议。
- 单厨房 2 个大火位 + 2 个小火位的并行做菜计划。
- 多人、多厨房、设备差异、延误后的滚动重排。
- 使用 OR-Tools CP-SAT 建模 RCPSP / Flexible Job Shop。

## 7. 下一步执行顺序

P0：

1. 建立 14 条现有记录的闭环盘点表，确认哪些已经包含个人版本和真实做菜日志。
2. 补齐至少 10 道真实菜谱闭环。
3. 在手机上完整验证登录、观看原视频、手动录入、编辑、厨房模式、日志、退出和重新打开。
4. 用第二个真实账号复测 RLS 和缓存隔离，再决定是否开启新用户注册。
5. 完成数据库 JSON 与 Storage 的备份恢复演练。
6. 到德国后使用当地 Wi-Fi 和移动网络验收正式域名。

P1：

1. 合并导入中心与手动录入入口。
2. 完善标签搜索、食材词典、翻译采购和购物清单数据结构。
3. 完善断网、超时、重复提交、空数据和保存失败的提示与重试。
4. 明确 GitHub 与 Sites 的同步规则，避免两个代码源长期分叉。
5. 开放注册前配置和验证 CAPTCHA、邮箱确认、限流及 SMTP。

P2：

1. 评估 Web/PWA 直接使用与微信小程序封装的限制。
2. 进入 V3 后再实施自动化、OCR、批量 AI 和多平台导入。

## 8. 历史进程摘要

### 2026-08-02：项目和数据库基础

- 从 B 站“吃饭的收藏夹”构思个人菜谱 Web。
- 确定 Supabase 作为数据库和登录服务。
- 完成主要页面、数据表、RLS、导入和备份设计。
- 建立收藏夹本地 JSON 导出方式。

### 2026-08-03 至 2026-08-04：真实数据、导入审计和域名

- 创建 Supabase 项目并运行 migration。
- 完成前 10 条元数据导入审计和 BV 去重设计。
- 购买腾讯云域名 `caoxiaoysm.com`，讨论子域名、DNS、HTTPS 和 ICP 备案。
- 明确域名只是地址，网页托管、数据库和 DNS 分属不同服务。

### 2026-08-09：内容审核和手动录入

- 增加视频审核导航和 B 站官方播放器。
- 增加手动菜谱录入工作台和原子保存函数。
- 对两个字幕样本进行验证。
- 加固数据库触发器权限。

### 2026-08-10：RLS、安全、部署和正式域名

- 完成 115 项真实 RLS 越权测试并清理测试数据。
- 修复跨用户关联和匿名读取边界。
- 部署 Sites，配置 Supabase 生产环境并绑定 `cookingapp.caoxiaoysm.com`。
- 取消 ChatGPT 门禁，但保留应用自身登录和 RLS。
- 增加密码登录、密码找回和恢复回调。
- 修复生产环境读取 Supabase 配置的问题。
- 优化会话恢复，避免每次打开都像整站重新启动。
- 增加多用户注册界面；新用户注册开关仍由 Supabase 控制。

### 2026-08-11：账号防护、手机导航和版本制度

- 加入 Turnstile Site Key 和 CAPTCHA token 链路。
- 完善注册、密码修改、密码重置和账号控制。
- 本地缓存按用户隔离，退出后不保留私人内容。
- 手机端确定五个主入口，并把其他入口放入“更多”。
- 明确 V2 以 B 站原视频 + 手工录入为核心，自动与批量推迟到 V3。
- 建立 `v1.0.0+20260811.01` 起始版本和统一日志规范。

## v1.0.0+20260811.01 - 2026-08-11

首个采用统一版本规范记录的基线版本。

### Added

- SemVer + 日期构建号版本制度。
- 手机端“前五个主入口 + 更多菜单”的完整导航策略。
- Turnstile 覆盖登录、注册与密码找回的验证链路。
- 本文件对项目从构思到当前状态的统一总结。

### Changed

- V2 聚焦可直接使用的 B 站原视频 + 手工录入流程。
- 自动化、OCR 和批量导入移至 V3。
- 路线图以真实验收结果为准，不再只按功能代码或数据库记录数判断完成。

### Security

- 保持 RLS 用户所有权隔离和退出清缓存。
- Site Key 可公开；Secret Key 只能存放在 Supabase 服务端配置。
- 新用户注册是否开放继续由 Supabase 后台开关控制。

### Validation

- 既有 RLS 越权矩阵：`115/115` 通过。
- 最近一次账号与前端构建检查：25 项功能/安全测试通过。
- Sites 侧手机完整导航与 V2 路线图已经形成独立提交。

### Known limitations

- 14 条记录尚不能证明 10 道菜全部完成真实闭环。
- GitHub 与 Sites 是两个代码来源，需要继续做可追溯同步。
- 中国大陆对 Sites 托管链路的访问可能不稳定。
- 自动录入、批量处理、购物清单和完整德国采购模块尚未完成。

