"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { useCooking } from "@/components/CookingProvider";
import { getSupabase } from "@/lib/supabase";

export default function SettingsPage(){const {isDemo,cloudError,recipes,logs,ingredients,resetDemo}=useCooking();const [email,setEmail]=useState("");useEffect(()=>{getSupabase()?.auth.getUser().then(({data})=>setEmail(data.user?.email||""))},[]);const exportAll=()=>{const blob=new Blob([JSON.stringify({exportedAt:new Date().toISOString(),recipes,logs,ingredients},null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`cookingapp-backup-${new Date().toISOString().slice(0,10)}.json`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)};return <div className="page"><header className="page-head"><div><p className="eyebrow">SETTINGS & BACKUP</p><h1>设置与数据</h1><p className="subtitle">检查数据库连接、登录状态，并随时导出自己的数据。</p></div></header><div className="two-col"><section className="panel"><h2>数据库连接</h2><div className="stats" style={{gridTemplateColumns:"repeat(2,1fr)",marginBottom:16}}><div className="stat"><div className="stat-label">当前模式</div><div style={{fontWeight:800}}>{isDemo?"浏览器演示数据":"Supabase 云端数据"}</div></div><div className="stat"><div className="stat-label">登录用户</div><div style={{fontWeight:800,wordBreak:"break-all"}}>{email||"未登录"}</div></div></div>{cloudError&&<div className="notice" style={{background:"#fbe5de",color:"#923c29"}}>云端同步提示：{cloudError}</div>}<p className="subtitle">连接方法：复制 <code>.env.example</code> 为 <code>.env.local</code>，填写 Project URL 与 Publishable key，然后重启开发服务器。</p><Link href="/login" className="btn btn-primary" style={{marginTop:15}}>登录 / 发送魔法链接</Link>{email&&<button className="btn btn-secondary" style={{marginTop:15,marginLeft:9}} onClick={async()=>{await getSupabase()?.auth.signOut();location.reload()}}>退出登录</button>}</section><aside className="panel"><h2>备份与重置</h2><p className="subtitle">导出包括菜谱、个人版本、日志和食材词典，不包括账号密钥。</p><button className="btn btn-primary" style={{width:"100%",marginTop:15}} onClick={exportAll}>导出完整 JSON 备份</button><button className="btn btn-danger" style={{width:"100%",marginTop:9}} onClick={()=>{if(confirm("确定恢复演示数据吗？本机当前演示修改会被覆盖。"))resetDemo()}}>恢复演示数据</button></aside></div></div>}
