"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function Login(){const[email,setEmail]=useState(""),[msg,setMsg]=useState("");const login=async()=>{const sb=createClient();if(!sb){setMsg("当前是演示模式：请先在 .env.local 填写 Supabase 配置。");return}const{error}=await sb.auth.signInWithOtp({email,options:{emailRedirectTo:location.origin}});setMsg(error?error.message:"登录链接已经发送到邮箱，请查收。")};return <div className="panel settings"><p className="eyebrow">ADMIN LOGIN</p><h1>登录管理端</h1><p>无需密码，Supabase 会发送一次性登录链接。</p><label>邮箱<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><button className="button primary" onClick={login}>发送登录链接</button>{msg&&<p className="notice">{msg}</p>}</div>}
