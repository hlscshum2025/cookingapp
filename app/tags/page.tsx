"use client";import {useStore} from "@/lib/store";export default function Tags(){const{recipes}=useStore();const tags=[...new Set(recipes.flatMap(r=>r.tags))];return <><div className="page-head"><div><p className="eyebrow">TAGS</p><h1>标签管理</h1></div></div><div className="tag-cloud">{tags.map(t=><span key={t}>{t}<b>{recipes.filter(r=>r.tags.includes(t)).length}</b></span>)}</div><div className="panel"><h2>推荐维度</h2><p>餐次 · 菜系/风格 · 主食材 · 烹饪方法 · 时间 · 难度 · 饮食目标 · 验证状态</p></div></>}
