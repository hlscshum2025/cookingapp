"use client";import {useRef,useState} from "react";import {parseBilibiliExport} from "@/lib/bilibili";import {useStore} from "@/lib/store";
export default function Imports(){const{sources,importSources}=useStore();const[input,setInput]=useState("");const[msg,setMsg]=useState("");const ref=useRef<HTMLInputElement>(null);const run=()=>{try{const videos=parseBilibiliExport(JSON.parse(input));const r=importSources(videos);setMsg(`成功新增 ${r.added} 条，跳过重复 ${r.duplicates} 条。`)}catch(e){setMsg(`导入失败：${e instanceof Error?e.message:"文件格式不正确"}`)}};return <><div className="page-head"><div><p className="eyebrow">IMPORT CENTER</p><h1>B 站收藏夹导入</h1></div></div><div className="form-grid"><section className="panel"><h2>1. 选择导出的 JSON</h2><p>只读取公开视频元数据，不需要 Cookie，也不会下载视频。</p><input ref={ref} type="file" accept="application/json,.json" onChange={async e=>{const f=e.target.files?.[0];if(f)setInput(await f.text())}}/><textarea className="json" value={input} onChange={e=>setInput(e.target.value)} placeholder="也可以把 JSON 内容粘贴到这里…"/><button className="button primary" onClick={run} disabled={!input}>预览并导入</button>{msg&&<p className="notice">{msg}</p>}</section><section className="panel"><h2>2. 导入队列</h2><strong className="big-number">{sources.length}</strong><p>条来源视频已进入待整理区</p><div className="source-list">{sources.slice(0,8).map(v=><a key={v.externalId} href={v.url} target="_blank"><span>{v.title}</span><small>{v.uploaderName||v.externalId}</small></a>)}</div></section></div></>}
